# Test fixture: L1 Framework config
metadata:
  framework_name: "LMAS-FullStack"
  framework_version: "3.12.0"

resource_locations:
  agents: ".lmas-core/development/agents"
  tasks: ".lmas-core/development/tasks"

performance_defaults:
  max_concurrent_operations: 4
  cache_ttl_seconds: 300

utility_scripts_registry:
  scripts_base: ".lmas-core/development/scripts"

ide_sync_system:
  enabled: true
  targets:
    - ".claude/CLAUDE.md"
